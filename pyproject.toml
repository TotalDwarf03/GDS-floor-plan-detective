[tool.poetry]
name = "src"
version = "0.1.0"
description = "A Streamlit application that analyzes floor plans in PDF format to check for required elements, providing a fun and engaging detective-themed interface."
authors = ["Your Name <you@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
streamlit = "^1.45.0"
pdf2image = "^1.17.0"
dotenv = "^0.9.9"
openai = "^1.76.2"
pyttsx3 = "^2.98"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
